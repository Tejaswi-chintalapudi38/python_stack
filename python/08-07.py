#Set--> Unordered, Mutable, Finite, Unique elements enclosed with {} but set itself as mutable but the elements are must be immutable we can't define the mutable elements in set
set1 = {1, 4, 5, 2, 'str1', 'str2', 4, 1}
print(len(set1))
print(set1)
list1 = [1, 2, 22, 22, 'str1', 'str1', 55]
print(list(set(list1)))

#Frozenset => immutable, Unordered, Finite, Unique but we can't define the set in frozenset 
frozenset1 = frozenset([1, 2, 3, 'str1', 'str2', 2])

#Boolean => To check the facts by using the True or False(Case sensitive)
print(2>3)
print(3>2)
print(-5>0)
print(-5<=-5)

#None => empty
a = None
print(a)
print(type(a))

#what is html
#what is hypertext
#what is markup
#what is a proper & basic structure of html
#what if i write the heading tag on header section or ater enclosing html section what it would be displayed
